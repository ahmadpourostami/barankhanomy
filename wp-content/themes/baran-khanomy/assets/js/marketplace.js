document.addEventListener('DOMContentLoaded',function(){const root=document.querySelector('[data-bk-marketplace]');if(!root)return;const track=root.querySelector('.bk-market-track');const cards=track?Array.from(track.querySelectorAll('.bk-market-card')):[];const dots=document.querySelector('[data-bk-market-dots]');let index=0,timer=null;const perView=()=>window.innerWidth<=760?1:(window.innerWidth<=1000?2:4);const pages=()=>Math.max(1,Math.ceil(cards.length/perView()));const renderDots=()=>{if(!dots)return;dots.innerHTML='';for(let i=0;i<pages();i++){const b=document.createElement('button');b.type='button';b.className=i===index?'is-active':'';b.setAttribute('aria-label','نمایش محصولات '+(i+1));b.addEventListener('click',()=>{index=i;render();restart()});dots.appendChild(b)}};const render=()=>{if(!track||!cards.length)return;const count=perView();const gap=parseFloat(getComputedStyle(track).gap)||0;const width=(track.clientWidth-(count-1)*gap)/count;track.style.transform='translateX('+((index*(width+gap)*count))+'px)';renderDots()};const restart=()=>{if(timer)clearInterval(timer);if(cards.length>perView())timer=setInterval(()=>{index=(index+1)%pages();render()},5500)};window.addEventListener('resize',()=>{index=Math.min(index,pages()-1);render();restart()});render();restart()});